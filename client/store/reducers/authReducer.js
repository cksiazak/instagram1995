const initialState = {
  userAuthed: false,
  loginError: false,
  signUpError: false
};

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
